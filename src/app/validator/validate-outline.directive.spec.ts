import { ValidateOutlineDirective } from './validate-outline.directive';

describe('ValidateOutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateOutlineDirective();
    expect(directive).toBeTruthy();
  });
});
